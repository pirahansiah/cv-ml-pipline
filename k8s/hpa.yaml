apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: cv-ml-api
  labels:
    app: cv-ml-api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: cv-ml-api
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
